package baseline;

public class MultiplicationTableGenerator {
    private int number1;
    private int number2;
    private String multiplicationTable;

    public MultiplicationTableGenerator() {
        // Intialize number1 to 1
        // Intialize number2 to 12
        // Intialize multiplicationTable to ""
    }

    public void outputMultiplicationTable() {
        String temp;
        // for(i = 1-12)
            // for(j = 1-12)
                // print i * j
                // temp = Parse to string(i * j)
                // multiplicationTable = multiplicationTable + temp
            // print "\n"
            // multiplicationTable = multiplicationTable + "\n"
    }

    public String getMultiplicationTable() {
        return multiplicationTable;
    }
}
